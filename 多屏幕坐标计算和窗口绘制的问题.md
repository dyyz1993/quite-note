# 多屏幕坐标计算和窗口绘制问题

## 问题背景

在开发 macOS 窗口高亮功能时，遇到了一个复杂的多屏幕坐标转换问题。需要实现的功能是：在所有屏幕上绘制覆盖层，当鼠标移动到某个窗口时，在该窗口周围显示蓝色虚线边框。

**核心问题**：蓝色边框的位置与实际窗口位置严重偏移，甚至需要在其他屏幕才能看到高亮框。

## 根本原因分析

### macOS 的三种坐标系统

macOS 存在三种不同的坐标系统，它们使用不同的原点和 Y 轴方向：

#### 1. **AppKit 坐标系**（Cocoa）
- **原点**：主显示器**左下角** `(0, 0)`
- **Y 轴方向**：**向上**增长
- **用途**：`NSScreen.frame`、`NSWindow.frame`、`NSEvent.mouseLocation`
- **特点**：符合数学坐标系直觉，但与屏幕坐标系不同

#### 2. **CoreGraphics 坐标系**（Quartz）
- **原点**：主显示器**左上角** `(0, 0)`
- **Y 轴方向**：**向下**增长
- **用途**：`kCGWindowBounds`、`CGDisplayBounds`、`CGDisplayCreateImage`
- **特点**：与屏幕像素物理布局一致

#### 3. **全局显示坐标空间**（Global Display Coordinate Space）
- **原点**：主显示器**左上角** `(0, 0)`
- **Y 轴方向**：**向下**增长
- **用途**：统一的多显示器坐标空间
- **特点**：所有显示器都在同一个坐标系中，坐标可以是负数

### 错误的坐标转换

最初的代码错误地假设可以通过简单的公式在 AppKit 和 CoreGraphics 坐标系之间转换：

```swift
// ❌ 错误的转换方法
let globalHeight = NSScreen.screens.reduce(0) { max($0, $1.frame.maxY) }
let screenFrameInCG = CGRect(
    x: screen.frame.origin.x,
    y: globalHeight - screen.frame.maxY,  // 错误！会导致负数坐标
    width: screen.frame.width,
    height: screen.frame.height
)
```

**问题**：
1. 计算的 `globalHeight` 不准确
2. 转换公式在多屏幕环境下完全失效
3. 没有考虑到屏幕可能在不同象限（负坐标）

### 实际日志验证

用户环境的屏幕布局：
```
Built-in Retina Display: (0.0, 0.0, 1512.0, 982.0)     - 主屏幕
PHL 279C9: (817.0, 982.0, 1920.0, 1080.0)             - 上方右侧
27MP35: (-1103.0, 982.0, 1920.0, 1080.0)              - 上方左侧
```

窗口的 CoreGraphics 坐标：
```
Quite Note Dev 窗口: (-181.0, -819.0, 520.0, 640.0)
```

使用错误的转换：
```
屏幕(CG): (0.0, 1080.0, 1512.0, 982.0)     - Y 值错误
窗口(CG): (-181.0, -819.0, 520.0, 640.0)
相交: false                                 - 应该是 true！
```

## 正确的解决方案

### 核心发现：使用 `CGDisplayBounds` API

Apple 提供了 `CGDisplayBounds` API，它返回屏幕在**全局显示坐标空间**中的矩形。

**关键特性**：
- 返回的坐标与 `kCGWindowBounds` **使用相同的坐标系统**
- 原点都在主屏幕左上角，Y 轴向下
- **可以直接进行比较和运算**，无需任何转换！

### 正确的实现

#### 1. 判断窗口是否与屏幕相交

```swift
func shouldShowHighlight(for window: WindowInfo, in screen: NSScreen) -> Bool {
    // 获取屏幕的 displayID
    guard let displayID = screen.deviceDescription[NSDeviceDescriptionKey("NSScreenNumber")] as? CGDirectDisplayID else {
        return false
    }

    // CGDisplayBounds 返回全局显示坐标空间中的矩形
    // 与 kCGWindowBounds 使用相同的坐标系统，可以直接比较
    let screenBounds = CGDisplayBounds(displayID)
    return window.bounds.intersects(screenBounds)
}
```

#### 2. 将全局坐标转换为屏幕局部坐标

```swift
private func convertToLocalBounds(_ globalRect: CGRect) -> CGRect {
    guard let displayID = screen.deviceDescription[NSDeviceDescriptionKey("NSScreenNumber")] as? CGDirectDisplayID else {
        return .zero
    }

    // 两者都在同一个全局 CoreGraphics 坐标系中，直接相减
    let screenBounds = CGDisplayBounds(displayID)

    return CGRect(
        x: globalRect.origin.x - screenBounds.origin.x,
        y: globalRect.origin.y - screenBounds.origin.y,
        width: globalRect.size.width,
        height: globalRect.size.height
    )
}
```

### 验证结果

使用正确的 API 后：

```
[DEBUG] shouldShowHighlight: 屏幕=27MP35, 窗口=Quite Note Dev
[DEBUG]   窗口 bounds(CG): (-181.0, -819.0, 520.0, 640.0)
[DEBUG]   屏幕(CGDisplayBounds): (-1103.0, -1080.0, 1920.0, 1080.0)
[DEBUG]   相交: true  ✅ 正确！
[DEBUG坐标]   相对位置: (922.0, 261.0, 520.0, 640.0)  ✅ 正确！
```

## 技术要点总结

### ✅ 正确做法

1. **使用 `CGDisplayBounds` API** 获取屏幕在全局显示坐标空间中的位置
2. **直接比较** `kCGWindowBounds` 和 `CGDisplayBounds` 的结果
3. **简单减法** 计算相对位置：`全局坐标 - 屏幕原点 = 局部坐标`

### ❌ 常见错误

1. **不要手动转换** `NSScreen.frame` 和 `kCGWindowBounds`
2. **不要假设** 全局高度可以通过 `maxY` 计算
3. **不要忽略** 多屏幕布局中可能存在负坐标的情况

## 其他相关问题

### ESC 键无法退出高亮模式

**问题**：添加了全局键盘监听后，ESC 键仍然无法退出。

**原因**：NSPanel 覆盖层面板可能拦截了键盘事件。

**解决方案**：同时添加本地键盘监听作为备用：

```swift
// 全局键盘监听
keyboardMonitor = NSEvent.addGlobalMonitorForEvents(matching: .keyDown) { [weak self] event in
    if event.keyCode == 53 { // ESC
        self?.stopHighlight()
    }
}

// 本地键盘监听（备用）
NSEvent.addLocalMonitorForEvents(matching: .keyDown) { [weak self] event in
    if event.keyCode == 53 { // ESC
        self?.stopHighlight()
        return nil // 消费事件
    }
    return event
}
```

## 参考资料

### Apple 官方文档
- [CGDisplayBounds](https://developer.apple.com/documentation/coregraphics/1454802-cgdisplaybounds)
- [kCGWindowBounds](https://developer.apple.com/documentation/coregraphics/kcgwindowbounds)
- [NSScreen](https://developer.apple.com/documentation/appkit/nsscreen)

### Stack Overflow
- [Understanding NSWindow coordinates vs. CGWindowListCopyWindowInfo coordinates](https://stackoverflow.com/questions/4811060/) - Ken Thomases (269 赞同)
- [Multiple monitors coordinate system in macOS](https://stackoverflow.com/questions/17423299/)

### 相关文档
- [COORDINATE_SYSTEM_RESEARCH.md](./COORDINATE_SYSTEM_RESEARCH.md) - 详细的技术调研报告

## 适用场景

这个解决方案适用于以下场景：
- ✅ 窗口高亮和选择
- ✅ 截图功能（窗口截图、区域截图）
- ✅ 多屏幕环境下的窗口定位
- ✅ 任何需要在屏幕上精确绘制 UI 的场景

## 关键代码片段

### 完整的窗口高亮控制器

文件位置：`Sources/QuiteNote/UI/Screenshot/WindowHighlight/WindowHighlightController.swift`

核心特性：
- 使用 `CGDisplayBounds` 进行精确坐标计算
- 支持多屏幕覆盖层
- 实时鼠标窗口检测
- ESC 键退出功能

## 总结

多屏幕坐标转换问题的核心在于理解 macOS 的坐标系统差异。使用 `CGDisplayBounds` API 是最可靠的解决方案，因为它返回的坐标与 `kCGWindowBounds` 完全一致，避免了复杂的坐标转换计算。

**记住**：
- 🎯 **同一个坐标系直接比较** - `kCGWindowBounds` 和 `CGDisplayBounds` 都在全局显示坐标空间
- 🚫 **不要手动转换** AppKit 和 CoreGraphics 坐标系
- ✅ **使用官方 API** - `CGDisplayBounds` 是 Apple 提供的正确工具
