# 窗口高亮测试指南

## 快速开始

应用已经启动，现在就可以测试了！

## 测试步骤

### 1. 找到测试按钮

在主窗口的**左侧边栏**，你会看到几个按钮：

```
┌─────────┐
│ 📄 文本 │  筛选按钮
│ 📎 文件 │
│ 📁 文件夹│
│   ───   │  分隔线
│ 📷 截图 │  测试截图按钮
│ ⬜ 窗口 │  ✅ 窗口高亮按钮（新增）
│         │
│   🔥    │  热力图
└─────────┘
```

**点击 ⬜ 窗口按钮** 即可启动窗口高亮功能。

### 2. 测试要点

#### ✅ 多屏幕覆盖
- 移动鼠标到你的3个屏幕
- 检查每个屏幕是否都有半透明覆盖层
- 所有屏幕应该同时响应鼠标移动

#### ✅ 窗口识别
- 鼠标移动到不同窗口上
- 观察是否显示蓝色虚线边框
- 边框应该精确贴合窗口边界

#### ✅ 跨屏幕窗口
- 测试跨越多个屏幕的窗口
- 检查边框是否正确显示

#### ✅ 全屏应用
- 打开 VS Code 或其他应用的全屏模式
- 检查是否能识别全屏窗口
- 验证边框是否覆盖整个屏幕

#### ✅ 视觉样式
- 蓝色虚线边框（dash: [8, 6]）
- 白色内边框（增加对比度）
- 圆角 8px

### 3. 停止测试

**再次点击 ⬜ 窗口按钮** 或按 **ESC** 键即可停止高亮。

### 4. 自动清理

- 按 ESC 缩小到浮球时，会自动停止高亮
- 关闭主窗口时，会自动停止高亮

## 预期结果

| 测试项 | 预期结果 |
|--------|----------|
| 3个屏幕覆盖 | ✅ 所有屏幕都有覆盖层 |
| 鼠标跟随 | ✅ 边框实时跟随鼠标 |
| 窗口识别 | ✅ 正确识别所有窗口 |
| 全屏应用 | ✅ VS Code 全屏可识别 |
| 视觉效果 | ✅ 蓝色虚线 + 白色内边框 |

## 可能的问题

### 如果边框位置不对
可能是坐标转换问题，请告诉我：
- 哪个屏幕的边框位置错误
- 边框偏移了多少（大概方向）

### 如果某个屏幕没有覆盖层
可能是屏幕面板创建失败，请告诉我：
- 是主屏幕还是副屏幕
- 控制台是否有错误日志

### 如果窗口识别不准确
可能是窗口边界计算问题，请告诉我：
- 哪种类型的窗口识别失败
- 窗口是否在全屏状态

## 下一步

测试完成后，告诉我结果如何：
- ✅ 如果一切正常：我们继续修复截图功能
- ❌ 如果有问题：我会根据你的反馈调整代码

## 调试日志

如果遇到问题，查看控制台日志：

```bash
log stream --predicate 'process == "QuiteNote"' --level debug
```

关键日志：
- `[DEBUG WindowHighlight]` - 窗口高亮相关
- `[DEBUG] 屏幕` - 屏幕信息
- `[DEBUG] 找到窗口` - 窗口识别结果
